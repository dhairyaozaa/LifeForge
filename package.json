{
  "name": "lifeforge",
  "version": "2.0.0",
  "description": "AI-Powered Life Simulator — neural model trains locally, no API, no cloud",
  "main": "index.js",
  "scripts": {
    "start":   "node index.js",
    "retrain": "node index.js --retrain",
    "train":   "python3 model/train.py"
  },
  "dependencies": {
    "chalk": "^4.1.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
